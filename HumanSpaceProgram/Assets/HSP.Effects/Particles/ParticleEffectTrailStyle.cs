namespace HSP.Effects.Particles
{
    public enum ParticleEffectTrailStyle
    {
        /// <summary>
        /// Each particle has its own trail.
        /// </summary>
        Individual,
        /// <summary>
        /// The particles are connected with a common trail.
        /// </summary>
        Connected
    }
}