using UnityEngine;

namespace HSP.Trajectories
{
    /*public readonly struct StateVector2
    {
        public readonly Vector3Dbl position;
        public readonly Vector3Dbl velocity;
        public readonly Vector3Dbl acceleration;

        public StateVector2( Vector3Dbl position, Vector3Dbl velocity, Vector3Dbl acceleration )
        {
            this.position = position;
            this.velocity = velocity;
            this.acceleration = acceleration;
        }

        public StateVector2( Vector3Dbl position, Vector3Dbl velocity )
        {
            this.position = position;
            this.velocity = velocity;
            this.acceleration = Vector3Dbl.zero;
        }

        public static StateVector2 Lerp( StateVector2 a, StateVector2 b, double t )
        {
            return new StateVector2( 
                Vector3Dbl.Lerp( a.position, b.position, t ),
                Vector3Dbl.Lerp( a.velocity, b.velocity, t ), 
                Vector3Dbl.Lerp( a.acceleration, b.acceleration, t )
                );
        }
    }*/
}